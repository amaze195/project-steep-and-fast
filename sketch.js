function setup() {
  createCanvas(400, 400);
}

function draw() {
  background(255,0,0);
  ellipse(50, 50, mouseX, mouseY);
  ellipse(mouseX, mouseY, 50, 50);

  ellipse(

    bezier()
    quadraticVertex();
  
}
